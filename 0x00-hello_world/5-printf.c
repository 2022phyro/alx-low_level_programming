#include <stdio.h>
/**
 * prints the phrase "with proper grammar, but the outcome is a piece of art,"
 * main - uses the printf function to carry out this task
 *Return: 0 if properly executed else a non zero value
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
