#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include "main.h"
/**
 *_islower - checks if the character is in lower case
 *Descruptio:'a function'
 *Return: 1 if accurate
 */
int _islower(int c)
{
	if (islower(c))
	{
		_putchar('1')
	}
	else
	{
		_putchar('0')
	}
return (c);
}
